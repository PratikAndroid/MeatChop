<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codewithpratik.meatchop">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeatChop">
        <activity android:name=".VendorOrderDetail"></activity>
        <activity android:name=".Vendor" />
        <activity android:name=".DetailsMyOrder" />
        <activity android:name=".OrderDetails" />
        <activity android:name=".Thanks" />
        <activity android:name=".OTP" />
        <activity android:name=".MyProfile" />
        <activity android:name=".MyOrder" />
        <activity android:name=".Address" />
        <activity android:name=".ConfirmOrder" />
        <activity android:name=".CartActivity" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>